services:
  navidrome:
    image: deluan/navidrome:latest
    ports:
      - "4533:4533"
    restart: unless-stopped
    volumes:
      - "/tb_storage2/navidrome/data:/data"
      - "/tb_storage2/navidrome/music:/music:ro"
