services:
  gitlab:
    image: gitlab/gitlab-ce:latest
    hostname: git.osfb.dev
    restart: unless-stopped
    environment:
      GITLAB_OMNIBUS_CONFIG: |
        registry_external_url 'http://registry.git.osfb.dev:5005'
        gitlab_rails['registry_enabled'] = true
    ports:
      - "5005:5005"
      - "8000:80"
      - "22:22"
    volumes:
      - /tb_storage/gitlab/config:/etc/gitlab
      - /tb_storage/gitlab/data:/var/opt/gitlab
      - /tb_storage/gitlab/logs:/var/log/gitlab
    networks:
      - gitlab_net

  gitlab-runner:
    image: gitlab/gitlab-runner:alpine
    restart: unless-stopped
    depends_on:
      - gitlab
    volumes:
      - /tb_storage/runner/config/:/etc/gitlab-runner
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - gitlab_net

networks:
  gitlab_net:
