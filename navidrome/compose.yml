services:
  navidrome:
    image: deluan/navidrome:latest
    ports:
      - "4533:4533"
    restart: unless-stopped
    volumes:
      - "/tb_storage/navidrome/data:/data"
      - "/tb_storage/navidrome/music:/music:ro"
